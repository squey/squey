# Default build type is debug
if (CMAKE_BUILD_TYPE)
else()
	set(CMAKE_BUILD_TYPE "debug")
endif()

set(CMAKE_CONFIGURATION_TYPES debug release relwithdebinfo relprotect)

if (WIN32)
else()
	set(CMAKE_CXX_COMMON_FLAGS "-Wall")
	set(CMAKE_CXX_ARCH_FLAGS "-march=native")

	set(CMAKE_CXX_FLAGS_DEBUG "-g ${CMAKE_CXX_ARCH_FLAGS} ${CMAKE_CXX_COMMON_FLAGS}")
	set(CMAKE_CXX_FLAGS_RELEASE "-O3 ${CMAKE_CXX_ARCH_FLAGS} ${CMAKE_CXX_COMMON_FLAGS}")
	set(CMAKE_CXX_FLAGS_RELWITHDEBINFO "-g -O3 ${CMAKE_CXX_ARCH_FLAGS} ${CMAKE_CXX_COMMON_FLAGS}")
	set(CMAKE_CXX_FLAGS_RELPROTECT "-O3 ${CMAKE_CXX_ARCH_FLAGS} ${CMAKE_CXX_COMMON_FLAGS}")
endif()
