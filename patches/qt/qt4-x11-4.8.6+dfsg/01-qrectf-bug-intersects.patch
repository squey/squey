diff --git a/src/corelib/tools/qrect.cpp b/src/corelib/tools/qrect.cpp
index 5237c8f..6f5cad2 100644
--- a/src/corelib/tools/qrect.cpp
+++ b/src/corelib/tools/qrect.cpp
@@ -2308,14 +2308,18 @@ QRectF QRectF::operator&(const QRectF &r) const
 
 bool QRectF::intersects(const QRectF &r) const
 {
+    if (w == 0   ||
+        r.w == 0 ||
+        h == 0   ||
+        r.h == 0) // null rect
+        return false;
+
     qreal l1 = xp;
     qreal r1 = xp;
     if (w < 0)
         l1 += w;
     else
         r1 += w;
-    if (l1 == r1) // null rect
-        return false;
 
     qreal l2 = r.xp;
     qreal r2 = r.xp;
@@ -2323,8 +2327,6 @@ bool QRectF::intersects(const QRectF &r) const
         l2 += r.w;
     else
         r2 += r.w;
-    if (l2 == r2) // null rect
-        return false;
 
     if (l1 >= r2 || l2 >= r1)
         return false;
@@ -2335,8 +2337,6 @@ bool QRectF::intersects(const QRectF &r) const
         t1 += h;
     else
         b1 += h;
-    if (t1 == b1) // null rect
-        return false;
 
     qreal t2 = r.yp;
     qreal b2 = r.yp;
@@ -2344,8 +2344,6 @@ bool QRectF::intersects(const QRectF &r) const
         t2 += r.h;
     else
         b2 += r.h;
-    if (t2 == b2) // null rect
-        return false;
 
     if (t1 >= b2 || t2 >= b1)
         return false;
