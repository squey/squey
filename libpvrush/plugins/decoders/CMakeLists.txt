SET(DECODERS_DIST_LIST
decoder_url
)

# URL
add_library(decoder_url SHARED url/url.cpp)
target_link_libraries(decoder_url ${PVCORE_LIBRARY} ${QT_QTCORE_LIBRARY} ${PVFILTER_LIBRARY})

#INSTALL(TARGETS ${DECODERS_DIST_LIST} DESTINATION decoders)

INSTALL(TARGETS ${DECODERS_DIST_LIST} DESTINATION decoders)
